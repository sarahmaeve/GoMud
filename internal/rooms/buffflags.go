package rooms

// Buff flag strings used by rooms for visibility and display logic.
// Declared locally to avoid importing internal/buffs.
const (
	flagHidden     = "hidden"
	flagSeeHidden  = "see-hidden"
	flagSeeNouns   = "see-nouns"
	flagEmitsLight = "lightsource"
	flagTripping   = "tripping"
)
